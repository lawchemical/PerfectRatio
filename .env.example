# Example environment variables for Railway deployment
# Copy this to Railway's environment variables section

# REQUIRED: Supabase connection (from your Supabase project settings)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Required: Generate a secure API key for your app
API_KEY=your-secure-api-key-here

# Server Configuration
PORT=3000
NODE_ENV=production

# Cache Configuration
CACHE_TTL=86400 # 24 hours in seconds
CACHE_CHECK_PERIOD=600 # Check for expired cache every 10 minutes

# Optional: Redis Configuration (if using Railway Redis addon)
# REDIS_URL will be auto-populated by Railway if you add Redis